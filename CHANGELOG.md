# CHANGELOG

## v1.1.0 (2026-02-14)

### ✨ 新特性
#### 1. 待办事项工作流支持 (`create-todo`)
- **新增 API**：`/api/create-todo`，支持接收企微工作流参数创建待办任务。
- **智能解析**：
    - 支持解析车站列表（顿号分隔）、车站区间（起止站）和单站模式。
    - 自动识别用户身份（Userid > 姓名 > 管理员兜底）。
    - 内置 5 分钟去重逻辑，防止重复提交。

### ⚡️ 故障工单解析优化 (`parse-ticket`)
#### 1. 地点解析增强
- **线路识别**：新增对 1号线、6号线、西海岸快线（13号线）的识别能力，解决同名车站归属问题。
- **设备识别**：优化设备类型匹配算法，支持 "TVMI/II" 等特殊编码，修复了部分设备编号提取失败的问题。

#### 2. 数据映射与去重
- **字段补全**：新增 `arrival_time` (到达时间) 和 `fix_time` (修复时间) 的字段映射。
- **精准去重**：在去重逻辑中加入 `location` 校验，避免不同地点的相同故障描述被误判为重复。

### 🔧 基础设施
- **数据库连接**：`afcSupabase` 客户端升级为使用 `SERVICE_ROLE_KEY`，确保后台任务具有完整的数据库读写权限（绕过 RLS）。
